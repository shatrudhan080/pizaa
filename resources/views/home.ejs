<section class="hero py-16">
    <div class="container mx-auto flex items-center justify-between">
        <div class="w-1/2">
            <h6 class="text-lg"><em>Are you hungery</em></h6>
            <h1 class="text-6xl font-bold">Don't wait !</h1>
            <button class=" order-btn px-6 py-2 text-white font-bold mt-4 rounded-full">Order Now</button>
        </div>
        <div class="w-1/2">
            <img src="/img/hero.png" />
        </div>
    </div>

</section>
<section class="menu container mx-auto py-8">
    <h1 class="text-xl font-bold mb-8">All Pizza</h1>
    <div class="grid grid-cols-4 col-gap-12 row-gap-16">
        <% pizzas.forEach(function(pizza){ %>
            <div class="w-64">
                <div>
                    <img class="h-40 mx-auto mb-4" src="/img/<%= pizza.image %>" />
                </div>
                <div class="text-center">
                    <h2 class="mb-4 text-lg">
                        <%= pizza.name %>
                    </h2>
                    <span class=" size py-1 px-4 rounded-full uppercase text-xs"> <%= pizza.size %></span>
                </div>
                <div class="flex items-center justify-around mt-6 ">
                    <span class="text-bold text-lg"> <%= pizza.price %></span>
                    <button class="add-to-cart py-1 px-6 rounded-full flex items-center font-bold">
                     <span>+</span>
                     <span class="ml-4">Add</span>
                     </button>
                </div>
            </div>
            <% }) %>


    </div>

</section>